import { Outlet } from "@remix-run/react";

export default function PositionsRoute() {
  return (
    <div>
      {/* <h1>positions</h1>
      <main>
        <Outlet />
      </main> */}
    </div>
  );
}
